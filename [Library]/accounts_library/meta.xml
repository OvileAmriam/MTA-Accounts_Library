<meta>

    <info author="OvileAmriam" name="Accounts Library" type="Library" version="1.0.0"/>
    <oop>true</oop>

    <include resource="mysql_library"/>

    <!-- Exports -->
    <script src="exports/server.lua" type="server"/>

    <!-- Settings -->
    <script src="settings/shared.lua" type="shared" cache="false"/>
    <script src="settings/server.lua" type="server"/>

    <!-- Utilities -->
    <script src="utilities/server.lua" type="server"/>

    <!-- Params: getUserData(account_name, data) | Returns Data; Else false Boolean -->
    <export function="getUserData" type="server"/>

    <!-- Params: setUserData(account_name, data, value) | Returns Boolean -->
    <export function="setUserData" type="server"/>

    <!-- Params: addUserAccount(account_name) | Returns Boolean -->
    <export function="addUserAccount" type="server"/>

    <!-- Params: delUserAccount(account_name) | Returns Boolean -->
    <export function="delUserAccount" type="server"/>

    <!-- Params: getAllUserAccounts() | Returns Data; Else false Boolean -->
    <export function="getAllUserAccounts" type="server"/>

</meta>
